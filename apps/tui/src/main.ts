// TUI entry point — STUB.
//
// The interactive terminal dashboard (built on @opentui/core, consuming the
// server via @rp/api-client) lands in a later ticket. This stub exists so the
// package resolves against @rp/api-client and type-checks in the build graph.
import { ApiClient } from "@rp/api-client/client"

void ApiClient

console.log("Ranksta's Paradise TUI (skeleton) — not yet implemented.")
